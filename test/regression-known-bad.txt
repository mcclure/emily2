# Embarrassingly, doesn't work.
parse/blank.em

# Backslashes in quotes are not printed sensibly (otherwise works) (works in selfhost)
parse/literals.em

# Backslash newline is not supported yet (works in selfhost)
parse/endBackslash.em

# Both parsers get confused by lines which end with a comma, but in different ways
parse/endComma.em

# . at end of program (or before comma) is allowed
parse/fail/dotStray.em

# regression.py doesn't load these correctly
parse/unicode/utf16le.em
parse/unicode/utf16be.em

# This ought to fail, but succeeds, becuase stealing works across commas
macro/fail/steal.em
