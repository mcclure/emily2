# Embarrassingly, doesn't work.
reader/blank.em

# Backslashes in quotes are not printed sensibly (otherwise works) (works in selfhost)
reader/literals.em

# Backslash newline is not supported yet (works in selfhost)
reader/endBackslash.em

# Backslash comma is not properly caught as an error (works in selfhost)
reader/fail/backslashComma.em

# Both readerrs get confused by lines which end with a comma, but in different ways
reader/endComma.em

# . at end of program (or before comma) is allowed
reader/fail/dotStray.em

# regression.py doesn't load these correctly
reader/unicode/utf16le.em
reader/unicode/utf16be.em

# This ought to fail, but succeeds, becuase stealing works across commas
parser/fail/steal.em
