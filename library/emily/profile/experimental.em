# Manually loaded from minimal profile

export macro
	unaryMacro(601, "!")
	unaryMacro(601, "~")

	splitMacro(611, "+")
	splitMacro(611, "-")

	splitMacro(621, "*")
	splitMacro(621, "/")
	splitMacro(621, "%")

	splitMacro(631, "<")
	splitMacro(631, ">")
	splitMacro(631, "<=")
	splitMacro(631, ">=")

	splitMacro(641, "==")
	splitMacro(641, "!=")

	splitMacro(661, "%%")
